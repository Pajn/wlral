name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        cd wlral
        RUST_BACKTRACE=1 cargo build --verbose
    - name: Run tests
      run: |
        cd wrlal
        cargo test --verbose
